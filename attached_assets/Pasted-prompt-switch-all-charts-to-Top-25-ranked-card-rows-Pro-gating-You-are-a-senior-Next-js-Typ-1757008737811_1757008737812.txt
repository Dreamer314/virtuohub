prompt — switch all charts to Top 25 + ranked card rows + Pro gating

You are a senior Next.js + TypeScript + Tailwind engineer. Use existing tokens and the three modes. Do not add new colors.

Scope

Apply to the three charts on /community/lists:

VHUB 25 (creators)

Platforms Index (platforms)

Momentum 50 → rename to Momentum 25 (fastest rising creators)

A) Data limit and copy

Create a single constant and use it everywhere:

export const CHART_LIMIT = 25;


Ensure all chart fetchers slice to 25 before render:

getVhubCreators(), getPlatformsIndex(), getMomentumCreators() or equivalents

Apply limit at the selector level so pagination and SSR are predictable

Update visible copy and labels:

“VHUB 100” → “VHUB 25”

“Top 100 creators” → “Top 25 creators”

Momentum: “Momentum 50” → “Momentum 25” and “Fastest rising creators” → “Top 25 rising creators”

Platforms Index: “Top 25 platforms”

Pro gating rule:

Non-Pro users see Top 10 only, then a lock card that says
“Unlock the full Top 25 on VHUB Pro” with a CTA to upgrade

Pro users see all 25

B) Remove dashboard clutter

For all three charts:

Remove per-row Movement, Growth, Impact Score, Creator Earnings Index columns

Keep a single platform label per row:

If one platform → show its name as small muted text

If multiple → show “Cross-platform”

Keep the small “NEW” chip only on Momentum 25 for first-timers

C) Ranked card rows

Replace table rows with card rows for a billboard feel.

Shared row classes to define once:

.vh-row        { @apply grid grid-cols-[72px_1fr_auto] items-center gap-4 rounded-xl px-4 py-3 ring-1 ring-border/50 bg-surface/70 hover:bg-surface/80 transition; }
.vh-rank       { @apply flex items-center gap-2 text-3xl font-extrabold tabular-nums; }
.vh-platform   { @apply text-sm opacity-80; }
.vh-row-top1   { @apply ring-accent/50; }
.vh-row-top2   { @apply ring-border/70; }
.vh-row-top3   { @apply ring-border/60; }


Render rules:

Column layout: Rank | Name (avatar + name, optional tiny tagline) | Platform label

Rank cell shows the numeric rank. If rank === 1 also render a crown icon next to the “1”

Rows 1–3 get the highlight rings shown above

Whole row is focusable and clickable with a visible focus outline

Keep rows compact on mobile. On small screens stack platform under the name

Example row JSX:

<li className={`vh-row ${i===0?'vh-row-top1':i===1?'vh-row-top2':i===2?'vh-row-top3':''}`}>
  <div className="vh-rank">
    {i===0 && <CrownIcon className="h-5 w-5 text-accent" aria-hidden />}
    {i+1}
  </div>
  <div className="flex items-center gap-3">
    <Avatar src={item.avatarUrl} alt="" className="h-10 w-10 rounded-full" />
    <div>
      <div className="font-semibold">{item.name}</div>
      {item.tagline ? <div className="text-xs opacity-70">{item.tagline}</div> : null}
    </div>
  </div>
  <div className="vh-platform">
    {item.platforms?.length === 1 ? item.platforms[0] : 'Cross-platform'}
  </div>
</li>

D) Platforms Index specific

Columns for Platforms Index:

Rank | Platform (logo + name) | Category (optional)

No extra metrics and no voice controls

E) Lists section hygiene

The filter chip row for “Featured Industry Lists” must sit under the section heading and subline

Remove Voice chips from that grid until wired

F) Tests

Unit: each chart renders max 25 items when Pro, and 10 + lock card when non-Pro

Snapshot: ranked card structure with three columns and no Movement or Impact columns

Accessibility: rows are keyboard focusable and announce title properly

Acceptance

All three charts show “Top 25” copy and render at most 25 results

Non-Pro gating shows Top 10 and a clean lock card

Rows are ranked cards with a crown + visible “1” on the first row

Only a single small platform label appears per row

Momentum 25 shows “NEW” where applicable

The Featured Lists filters are positioned under the section title