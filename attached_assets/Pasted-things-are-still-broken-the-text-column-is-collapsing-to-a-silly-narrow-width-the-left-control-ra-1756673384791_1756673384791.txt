things are still broken:

the text column is collapsing to a silly narrow width

the left control rail got inserted, but the hero grid didn’t reserve hard widths for each column, so everything floats around

let’s pin the layout so it cannot break, even if some global styles are fighting you. paste this exactly.

1) wrap the whole row in a hard 3-column grid
{/* Featured row shell */}
<div className="relative grid lg:[grid-template-columns:220px_minmax(980px,1fr)_360px] gap-6">
  {/* left control rail lives in col 1 (keep your current controls here) */}
  <aside className="hidden lg:flex col-[1] flex-col items-start justify-center gap-3 text-white/70">
    <p className="text-xs">Use ← → to navigate • Space to pause</p>
    <div className="flex flex-col gap-3">
      <button onClick={()=>go(-1)} className="h-12 w-12 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">‹</button>
      <button onClick={()=>go(1)}  className="h-12 w-12 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">›</button>
      <button onClick={()=>setPaused(p=>!p)} aria-pressed={paused}
              className="h-12 w-12 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">
        {paused ? "▶" : "⏸"}
      </button>
    </div>
    {/* dots */}
    <div className="mt-2 flex gap-2">
      {items.map((_,idx)=>(
        <button key={idx} onClick={()=>set(idx)} aria-label={`Go to slide ${idx+1}`}
          className={`h-2.5 w-2.5 rounded-full ${idx===i ? "bg-[#825FFF]" : "bg-white/25 hover:bg-white/40"}`} />
      ))}
    </div>
  </aside>

  {/* hero card sits in the middle column, locked */}
  <section id="featuredV2" className="col-[2] relative px-6 md:px-0 not-prose">
    {/* inner 2-col grid with hard minimums so the text can’t collapse */}
    <div className="relative grid gap-14 items-center md:[grid-template-columns:minmax(620px,1fr)_minmax(460px,560px)]">
      {/* media */}
      <div data-el="media" className="relative">
        <div className="absolute inset-0 -z-10 bg-[radial-gradient(closest-side,rgba(120,100,255,.18),transparent_70%)]" />
        <div className="w-full aspect-[16/9] min-h-[360px] rounded-2xl overflow-hidden shadow-[0_0_90px_rgba(120,100,255,.16)] ring-1 ring-white/8">
          <img src={item.imageSrc} alt={item.imageAlt} className="w-full h-full object-cover object-center" loading="lazy" />
        </div>
      </div>

      {/* text */}
      <div data-el="textcol" className="cq min-w-[460px] max-w-[560px]">
        <div className="text-sm text-white/55">{formatDate(item.dateISO)}</div>
        <div className="mt-2 flex items-center gap-2">
          <span className={`rounded-full px-3 py-1 text-xs font-medium ${chipClass(item.tag)}`}>{item.tag}</span>
          {item.type ? <span className="text-white/55 text-xs">• {item.type}</span> : null}
        </div>
        <h2 className="mt-3 text-balance text-pretty keep-words leading-[1.05] tracking-tight text-[clamp(2.25rem,6cqi,3.75rem)]">
          {noWidow(item.title)}
        </h2>
        <p className="mt-5 text-lg text-white/80">{item.blurb}</p>
        <a href={item.ctaHref} className="mt-6 inline-flex items-center justify-center rounded-xl px-5 py-3 bg-[#6E4BFF] hover:bg-[#825FFF] text-white font-medium">{item.ctaLabel}</a>
      </div>
    </div>
  </section>

  {/* right rail column is your existing sidebar space */}
  <div className="hidden lg:block col-[3]" />
</div>

2) add these tiny global utilities if you haven’t
#featuredV2 .text-balance{ text-wrap: balance; }
#featuredV2 .text-pretty{ text-wrap: pretty; }
#featuredV2 .cq{ container-type: inline-size; }
#featuredV2 .keep-words{ word-break: keep-all; }

3) remove the overlay arrows

Delete any absolute inset-y-0 left/right arrow overlay you still have. The only arrows should be in the left rail.

4) kill the common “layout clobberers”

Somewhere up the tree you likely have one or more of these. Nuke them for this section:

negative margins on the media wrapper: set to ml-0

global .prose or .content typography on this section: keep not-prose on #featuredV2

any parent with overflow-hidden clipping things

image or text wrappers with max-w-sm, w-80, or object-contain

5) quick sanity console check

Drop this effect temporarily in the component to prove the fix:

useEffect(()=>{
  const t = document.querySelector('#featuredV2 [data-el="textcol"]');
  const m = document.querySelector('#featuredV2 [data-el="media"]');
  if(t && m){
    console.table({
      textWidth: getComputedStyle(t).width,
      mediaWidth: getComputedStyle(m).width,
      grid: getComputedStyle(t.closest('[class*="grid"]')).gridTemplateColumns
    });
  }
},[]);


At desktop you want textWidth ≈ 460–560 px and mediaWidth ≥ 620 px.

this keeps your right-biased look, fills the left with useful controls, and hard-locks the text width so it never becomes that vertical column again.