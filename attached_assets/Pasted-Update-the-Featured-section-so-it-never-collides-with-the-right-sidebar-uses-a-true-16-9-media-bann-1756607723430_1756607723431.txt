Update the Featured section so it never collides with the right sidebar, uses a true 16:9 media banner, and sets a balanced headline.

Layout

Wrap the whole section in a container that reserves space for the right rail on large screens:

section#featured gets relative max-w-[1400px] mx-auto px-6 md:px-8 lg:pr-[380px]
// adjust 380px to your sidebar width

Inside, keep a two-column grid:

grid md:grid-cols-[1.75fr_1fr] gap-16 xl:gap-28 items-center

Media wrapper:

relative md:-ml-6 xl:-ml-12

Frame: w-full aspect-[16/9] min-h-[360px] xl:min-h-[440px] rounded-2xl overflow-hidden shadow-[0_0_90px_rgba(120,100,255,.16)] ring-1 ring-white/8

<img>: class="w-full h-full object-cover object-center"

Text column:

max-w-[36ch] md:mr-2

Date line small and muted

Tag chip + optional type

Title classes: text-5xl md:text-6xl leading-[1.05] tracking-tight [text-wrap:balance]

Blurb: text-lg text-white/80 mt-5

Arrows anchored to the section edges, not the image, and clear the sidebar:

Prev: absolute top-1/2 -translate-y-1/2 left-2 md:left-0 md:-ml-10 xl:-ml-16

Next: absolute top-1/2 -translate-y-1/2 right-2 lg:right-[400px] xl:right-[400px]
// right offset ~ sidebar width + padding

Button style: h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40 transition

Dots: mt-10

Kill width traps

Ensure no max-w-sm, w-80, object-contain, or fixed inline widths on the media container or <img>. Use only the classes above.

Data and copy rules

Tag uses: Creator Insights, Spotlight, Tips & Guides, Industry News, Tech

type is Interview | Article | Video and renders after the tag

Replace headshot with a 16:9 banner for this slide

Replace current JSX with this skeleton for each slide:

<section id="featured" className="relative max-w-[1400px] mx-auto px-6 md:px-8 lg:pr-[380px]">
  <div className="relative grid md:grid-cols-[1.75fr_1fr] gap-16 xl:gap-28 items-center">
    <div className="relative md:-ml-6 xl:-ml-12">
      <div className="absolute inset-0 -z-10 bg-[radial-gradient(closest-side,rgba(120,100,255,.18),transparent_70%)]" />
      <div className="w-full aspect-[16/9] min-h-[360px] xl:min-h-[440px] rounded-2xl overflow-hidden shadow-[0_0_90px_rgba(120,100,255,.16)] ring-1 ring-white/8">
        <img src={item.imageSrc} alt={item.imageAlt} className="w-full h-full object-cover object-center" loading="lazy" />
      </div>
    </div>

    <div className="max-w-[36ch] md:mr-2">
      <div className="text-sm text-white/55">{formatDate(item.dateISO)}</div>
      <div className="mt-2 flex items-center gap-2">
        <span className={`rounded-full px-3 py-1 text-xs font-medium ${chipClass(item.tag)}`}>{item.tag}</span>
        {item.type ? <span className="text-white/55 text-xs">• {item.type}</span> : null}
      </div>
      <h3 className="text-5xl md:text-6xl leading-[1.05] tracking-tight mt-3 [text-wrap:balance]">{item.title}</h3>
      <p className="text-lg text-white/80 mt-5">{item.blurb}</p>
      <a href={item.ctaHref} className="inline-flex items-center justify-center rounded-xl px-5 py-3 bg-[#6E4BFF] hover:bg-[#825FFF] text-white font-medium mt-6">{item.ctaLabel}</a>
    </div>

    <button aria-label="Previous" className="absolute top-1/2 -translate-y-1/2 left-2 md:left-0 md:-ml-10 xl:-ml-16 h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">‹</button>
    <button aria-label="Next" className="absolute top-1/2 -translate-y-1/2 right-2 lg:right-[400px] xl:right-[400px] h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">›</button>
  </div>
  <div className="mt-10 flex justify-center gap-3">{/* dots */}</div>
</section>


”

Replace the image and headline for this slide

Title options

MetaHuman into Houdini, now faster

MetaHuman to Houdini with fewer steps

Faster previz with the MetaHuman Houdini bridge

Image prompt (generate a 16:9 banner)
“Cinematic 16:9 banner of a realistic digital human on a neutral studio gradient, subtle rim light, soft bloom, glossy finish. Clean, no UI, no logos, extra negative space on both sides.”

That will make the image read large, keep the headline from towering, and keep everything clear of the right rail. If the sidebar width differs, tweak lg:pr-[380px] and lg:right-[400px] together by the same amount.