You are editing a Next.js + TypeScript + Tailwind project that has light/dark/charcoal modes.

Goal

Make “Featured Industry Lists” section readable in dark/charcoal:

Title uses Orbitron and is larger.

Chips and Sort <select> use tokenized colors (no white-on-white).

Card covers show a real image if available, otherwise a branded placeholder.

We will confirm the correct file path by adding a small, temporary dev label.

Tokens

Use only existing tokens: bg-surface, text-foreground, ring-border, accent. Do not hardcode whites.

STEP 1 — Locate files actually used by the Featured Lists grid

Search the repo for the JSX that renders the “Featured Industry Lists” heading. It should be in one of:

components/FeaturedLists.tsx

app/(community)/lists/FeaturedLists.tsx

app/community/lists/FeaturedLists.tsx

Open the file that contains the actual heading text, then:

A. Replace the section header block with:

<div className="mb-6">
  <div className="text-xs tracking-wide text-foreground/70">COMMUNITY LISTS</div>
  <h2 className="font-orbitron text-4xl md:text-5xl font-extrabold tracking-tight text-foreground">
    Featured Industry Lists
  </h2>
  <p className="mt-2 text-foreground/80">
    Editor-led and data-driven rankings, roundups, and best-of lists.
  </p>
  {/* DEV: remove after verification */}
  <span data-dev-id="featured-lists-verified" className="mt-2 inline-block text-xs px-2 py-0.5 rounded bg-accent/15 text-accent ring-1 ring-accent/30">
    FeaturedLists verified
  </span>
</div>


B. Normalize the chips and Sort select
Find the chip component used right above the grid. If there’s a shared one (e.g., components/ui/Pill.tsx), open that instead. Otherwise update the local chip mapping in this file.

Add/replace this helper:

function chipClass(active: boolean) {
  return [
    'px-3 py-1.5 rounded-full text-sm',
    'ring-1 ring-border/50',
    active
      ? 'bg-accent/15 text-accent ring-accent/40'
      : 'bg-surface/80 text-foreground/80 hover:bg-surface/90'
  ].join(' ')
}


Update the Sort <select> className everywhere in this section to:

const selectClass =
  'rounded-lg bg-surface/80 text-foreground ring-1 ring-border/50 ' +
  'px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-accent/40 ' +
  'placeholder:text-foreground/60';


and set className={selectClass} on the actual <select>.

If you find a shared Select (e.g., components/ui/Select.tsx) that these pages use, update it there with the same classes.

STEP 2 — Fix card cover images (or show a placeholder)

Find the list card component used by the grid. Typical names:

components/ListCard.tsx

components/lists/ListCard.tsx

Open the actual component responsible for the image banner (the element with aspect-[16/9] or the first child in the card) and replace that part with:

import Image from 'next/image';

function Cover({ src, alt }: { src?: string; alt: string }) {
  return (
    <div className="relative aspect-[16/9] overflow-hidden bg-surface/80">
      {src ? (
        <Image
          src={src}
          alt={alt}
          fill
          sizes="(min-width: 1024px) 33vw, 100vw"
          className="object-cover"
          unoptimized
        />
      ) : (
        <div className="absolute inset-0 bg-[radial-gradient(transparent,transparent),linear-gradient(180deg,rgba(255,255,255,0.03),rgba(255,255,255,0))]">
          <div className="absolute inset-0 opacity-[0.06]" />
        </div>
      )}
    </div>
  );
}


Then ensure the card uses it:

<a ... className="group block rounded-2xl overflow-hidden ring-1 ring-border/50 bg-surface/80 hover:bg-surface/90 transition">
  <Cover src={item.coverUrl} alt={item.title} />
  <div className="p-4 text-foreground">{/* title/meta here */}</div>
</a>


Important: If the card was previously using <img>, remove it and use Cover instead.
We pass unoptimized temporarily so images render even if next.config.js remote patterns aren’t set yet.

STEP 3 — Ensure pills/badges are on-brand

If there’s a small “type” or “voice” badge component (e.g., components/ui/Badge.tsx), set:

export function typeBadgeClass() {
  return 'px-2 py-0.5 rounded-full text-xs bg-surface/90 text-foreground/80 ring-1 ring-border/50';
}
export function voiceBadgeClass(voice: 'editorial'|'user') {
  return voice === 'editorial'
    ? 'px-2 py-0.5 rounded-full text-xs bg-accent/15 text-accent ring-1 ring-accent/30'
    : 'px-2 py-0.5 rounded-full text-xs bg-surface/90 text-foreground/80 ring-1 ring-border/50';
}


Replace any hardcoded text-white/bg-white with tokenized text-foreground & bg-surface.

STEP 4 — Verify

Reload the page. You should see the small “FeaturedLists verified” label above the section (remove later).

The Sort select text is visible in charcoal mode.

Cards show a cover image or a subtle placeholder (no pure-white boxes).

Pills and badges match the palette (no rainbow chips).

If you still see white-on-white text, locate the exact select component used in this section (search for the JSX with the “Most Recent / Most Viewed / Highest Rated” options) and apply className={selectClass} right there.

STEP 5 — Clean-up (after verifying)

Remove the <span data-dev-id="featured-lists-verified" ...> label.