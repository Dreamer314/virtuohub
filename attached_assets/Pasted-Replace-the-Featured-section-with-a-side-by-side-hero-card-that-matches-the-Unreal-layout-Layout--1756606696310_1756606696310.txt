Replace the Featured section with a side-by-side hero card that matches the Unreal layout:

Layout

Outer: relative max-w-[1200px] mx-auto px-6

Card: grid md:grid-cols-[1.25fr_1fr] gap-16 xl:gap-24 items-center

Media frame (left): aspect-[16/9] w-full rounded-2xl overflow-hidden shadow-[0_0_80px_rgba(120,100,255,.18)] ring-1 ring-white/8

Image tag uses class="h-full w-full object-cover" and loading="lazy"

Add a faint radial background behind the frame: absolute div with pointer-events-none and bg-[radial-gradient(closest-side,rgba(120,100,255,.18),transparent_70%)] behind the image

Text column (right): max-w-[48ch]

Date: small, muted. text-sm text-white/55 mb-2 formatted from ISO date

Tag chip: pill with color by tag (see map), then a small middot and optional type label (Interview, Article, Video)

Title: text-5xl leading-tight font-extrabold tracking-tight mt-3

Blurb: text-lg text-white/80 mt-4

CTA: primary button mt-6

Arrows

Prev and Next are outside the media frame and vertically centered to the entire card

Containers:

Prev absolute left-0 md:-left-10 xl:-left-16 top-1/2 -translate-y-1/2

Next absolute right-0 md:-right-10 xl:-right-16 top-1/2 -translate-y-1/2

Button style: h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40 transition

Disable during slide transition to avoid double clicks

Autoplay and dots

Keep autoplay and the pause-on-hover you already have

Dots stay below card. Increase top margin to mt-8 so they do not crowd the CTA

Data shape

type FeaturedItem = {
  id: string
  dateISO: string
  tag: 'Creator Insights' | 'Spotlight' | 'Tips & Guides' | 'Industry News' | 'Tech'
  type?: 'Interview' | 'Article' | 'Video'
  title: string
  blurb: string
  ctaLabel: string
  ctaHref: string
  imageSrc: string
  imageAlt: string
}


Tag chip styles

Creator Insights → chip text #7AF3CF, bg rgba(120,243,207,.12)

Spotlight → text #C5A8FF, bg rgba(197,168,255,.12)

Tips & Guides → text #B6E86E, bg rgba(182,232,110,.12)

Industry News → text #6EA8FF, bg rgba(56,132,255,.12)

Tech → text #FFC888, bg rgba(255,196,136,.10)

HTML structure (use this JSX skeleton)

<section id="featured" className="relative max-w-[1200px] mx-auto px-6">
  <div className="relative grid md:grid-cols-[1.25fr_1fr] gap-16 xl:gap-24 items-center">
    {/* Media */}
    <div className="relative">
      <div className="absolute inset-0 -z-10 bg-[radial-gradient(closest-side,rgba(120,100,255,.18),transparent_70%)]" />
      <div className="aspect-[16/9] w-full rounded-2xl overflow-hidden shadow-[0_0_80px_rgba(120,100,255,.18)] ring-1 ring-white/8">
        <img src={item.imageSrc} alt={item.imageAlt} className="h-full w-full object-cover" loading="lazy" />
      </div>
    </div>

    {/* Text */}
    <div className="max-w-[48ch]">
      <div className="text-sm text-white/55">{formatDate(item.dateISO)}</div>
      <div className="mt-2 flex items-center gap-2">
        <span className={`tag-chip tag-${slug(item.tag)}`}>{item.tag}</span>
        {item.type ? <span className="text-white/55">• {item.type}</span> : null}
      </div>
      <h3 className="text-5xl leading-tight font-extrabold tracking-tight mt-3">{item.title}</h3>
      <p className="text-lg text-white/80 mt-4">{item.blurb}</p>
      <a href={item.ctaHref} className="inline-flex items-center justify-center rounded-xl px-5 py-3 bg-[#6E4BFF] hover:bg-[#825FFF] text-white font-medium mt-6">{item.ctaLabel}</a>
    </div>

    {/* Arrows */}
    <button aria-label="Previous" className="absolute left-0 md:-left-10 xl:-left-16 top-1/2 -translate-y-1/2 h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">{'‹'}</button>
    <button aria-label="Next" className="absolute right-0 md:-right-10 xl:-right-16 top-1/2 -translate-y-1/2 h-12 w-12 md:h-14 md:w-14 rounded-full bg-white/6 backdrop-blur ring-1 ring-white/15 hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40">{'›'}</button>
  </div>

  {/* Dots under card */}
  <div className="mt-8 flex justify-center gap-3">...</div>
</section>


Rules

Replace any tag “News” with “Industry News”

Never use “Interview” as a tag. Use it as type

Do not let the headline wrap under 2 words per line at desktop. Use text-balance if supported

Spacing checklist

If image and text still feel tight, bump grid to [1.35fr_1fr] and xl:gap-28

Add pt-2 to the text column to align the headline’s cap height with the image’s optical center

Keep existing autoplay and data. Only refactor the Featured section.”